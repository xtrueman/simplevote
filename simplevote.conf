# Конфигурация Nginx для SimpleVote
# Домен: vote.llmtech.ru
# Расположение: /etc/nginx/sites-available/simplevote.conf

upstream simplevote_backend {
    server 127.0.0.1:8000;
}

server {
    listen 80;
    server_name vote.llmtech.ru;

    # Корневая директория для статических файлов
    root /srv/simplevote;

    # Логирование
    access_log /var/log/nginx/simplevote_access.log;
    error_log /var/log/nginx/simplevote_error.log;

    # Статические файлы (HTML, CSS, JS)
    location ~* \.(html|css|js)$ {
        try_files $uri =404;
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }

    # API endpoints - проксирование на FastAPI с префиксом /api/
    location /api/ {
        proxy_pass http://simplevote_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Главная страница
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Отключаем логирование для favicon
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }

    # Безопасность
    location ~ /\.(?!well-known) {
        deny all;
    }
}

# Раскомментируйте для HTTPS с Let's Encrypt:
# server {
#     listen 443 ssl http2;
#     server_name vote.llmtech.ru;
#
#     ssl_certificate /etc/letsencrypt/live/vote.llmtech.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/vote.llmtech.ru/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     root /srv/simplevote;
#     access_log /var/log/nginx/simplevote_access.log;
#     error_log /var/log/nginx/simplevote_error.log;
#
#     location ~* \.(html|css|js)$ {
#         try_files $uri =404;
#         expires 1h;
#         add_header Cache-Control "public, must-revalidate";
#     }
#
#     location /api/ {
#         proxy_pass http://simplevote_backend;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     location = /favicon.ico {
#         log_not_found off;
#         access_log off;
#     }
#
#     location ~ /\.(?!well-known) {
#         deny all;
#     }
# }
#
# server {
#     listen 80;
#     server_name vote.llmtech.ru;
#     return 301 https://$server_name$request_uri;
# }
